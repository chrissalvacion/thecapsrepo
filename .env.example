# Database
# Use the Supabase transaction pooler URL for Vercel/serverless deployments.
# Keep this URL clean (no sslmode query param) and control SSL via PGSSLMODE.
SUPABASE_DB_POOLER_URL=

# Optional fallbacks for local development
DATABASE_URL=
SUPABASE_DATABASE_URL=
PGHOST=
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=capsrepo
PGSSLMODE=require

# App
JWT_SECRET=
SERVER_PORT=3000
NODE_ENV=development
FRONTEND_URL=
